---
import { SEORenderer } from '@jjlmoya/utils-shared';
import { forensicFingerprintMinutiaeIdentifier } from './index';
import type { KnownLocale } from '../../types';

interface Props {
  locale?: KnownLocale;
}

const { locale = 'en' } = Astro.props;
const content = await forensicFingerprintMinutiaeIdentifier.i18n[locale]?.();
if (!content) return null;
---

{content.seo && content.seo.length > 0 && <SEORenderer content={{ locale, sections: content.seo }} />}
